总结当前对话中的关键信息，写入 MEMORY.md。

请回顾本次对话，提取以下内容并写入 MEMORY.md：
- 确定的实现方案和架构决策
- 关键文件路径和代码模式
- 遇到的坑或需要注意的点
- 当前进展（完成了什么，还剩什么）

写入格式要求：
- 用 ## 标题标明任务名
- 用简洁的要点列表，不要大段文字
- 只记结论，不记过程中的错误尝试

写完后运行 `agent-memory save` 保存快照。
